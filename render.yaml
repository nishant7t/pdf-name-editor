services:
  - type: web
    name: pdf-name-editor
    env: python
    buildCommand: apt-get install -y tesseract-ocr poppler-utils && pip install -r requirements.txt
    startCommand: gunicorn app:app
